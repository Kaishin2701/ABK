services:
  - type: web
    name: abk-product-checker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 2 --threads 4 --timeout 120 web_app:app
    autoDeploy: true
